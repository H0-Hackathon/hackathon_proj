# =====================================================
# CoastGuard — Supply Chain Monitor
# Backend Dependencies
# =====================================================

# --- Core Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
starlette>=0.41.0
sse-starlette>=3.0.0
anyio>=4.0.0
python-multipart>=0.0.21

# --- Data Validation ---
pydantic==2.11.10
pydantic-settings==2.10.1
pydantic_core==2.33.2

# --- Database (SQLite for Phase 1; Aurora PostgreSQL + pgvector for Phase 3) ---
# SQLite works out of the box — no extra driver needed.
SQLAlchemy>=2.0.36
alembic>=1.13.0

# PostgreSQL / Aurora + IAM auth
psycopg2-binary>=2.9.9
pgvector>=0.3.0
boto3>=1.34.0

# --- Environment & Utilities ---
python-dotenv>=1.0.0
numpy>=1.26.0

# --- WebSockets (for demo route) ---
websockets>=16.0

# --- HTTP Client ---
httpx>=0.28.0
requests>=2.32.0

# --- Platform-specific async event loop ---
uvloop>=0.22.0; platform_system != "Windows"

# --- AI / Agent Pipeline (Phase 2+) ---
# Uncomment when implementing the 5-agent pipeline with real Gemini calls:
# crewai>=1.7.0
# google-generativeai>=0.8.0
# langchain-google-genai>=2.0.0
# langchain>=0.3.0
# langchain-chroma>=0.2.0
# chromadb>=0.5.0
# sentence-transformers>=3.3.0
