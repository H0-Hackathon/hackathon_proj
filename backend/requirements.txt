# =====================================================
# CoastGuard — Supply Chain Monitor
# Backend Dependencies
#
# Quick start:
#   python -m venv .venv
#   .venv\Scripts\activate      (Windows)
#   source .venv/bin/activate   (macOS/Linux)
#   pip install -r requirements.txt
# =====================================================

# --- Core Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
starlette>=0.41.0
sse-starlette>=3.0.0
anyio>=4.0.0
python-multipart>=0.0.21

# --- Data Validation ---
pydantic==2.11.10
pydantic-settings==2.10.1
pydantic_core==2.33.2

# --- Database ---
# SQLite works out of the box (default). For Aurora PostgreSQL + pgvector + IAM auth:
SQLAlchemy>=2.0.36
alembic>=1.13.0
psycopg2-binary>=2.9.9
pgvector>=0.3.0
boto3>=1.34.0

# --- Environment & Utilities ---
python-dotenv>=1.0.0
numpy>=1.26.0

# --- WebSockets (for demo route) ---
websockets>=16.0

# --- HTTP Client ---
httpx>=0.28.0
requests>=2.32.0

# --- Platform-specific async event loop ---
uvloop>=0.22.0; platform_system != "Windows"

# --- AI / Agent Pipeline (5-agent CrewAI pipeline) ---
# Required to run with USE_MOCK_LLM=false (set GEMINI_API_KEY in .env).
# With USE_MOCK_LLM=true (default) these are unused but safe to install.
crewai>=1.7.0
google-generativeai>=0.8.0

# --- Optional: RAG / Vector search (Phase 3, not required for current pipeline) ---
# langchain-google-genai>=2.0.0
# langchain>=0.3.0
# langchain-chroma>=0.2.0
# chromadb>=0.5.0
# sentence-transformers>=3.3.0
